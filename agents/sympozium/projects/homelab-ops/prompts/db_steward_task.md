Check the stateful services now.

Call facts_postgres_health, facts_cache_health, facts_object_store_health,
facts_stream_health, facts_metrics_store_health and facts_volume_fill. Use
pg_analyze_db_health and pg_get_top_queries for query-level detail.

Then write the Status / Postgres / Cache / Object store / Streams / Metrics store
/ Room to grow report and deliver it as your Delivery section instructs.
