---
description: Author a design spec under docs/specs/ with the design-spec skill
agent: build
---

Load the `design-spec` skill with the skill tool, then use it to author a
design spec for the request below. Follow its phases in order and stop at every
checkpoint for review.

Request: $ARGUMENTS
