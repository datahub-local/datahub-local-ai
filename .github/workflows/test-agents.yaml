name: Test Agents

on:
  pull_request:
    paths:
      - ".github/workflows/test-agents.yaml"
      - "agents/mcp/**"
      - "agents/sympozium/**"
      - "pyproject.toml"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/test-agents.yaml"
      - "agents/mcp/**"
      - "agents/sympozium/**"
      - "pyproject.toml"

permissions:
  contents: read

concurrency:
  group: test-agents-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sympozium:
    name: Sympozium ensembles
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python with uv
        run: uv python install 3.13

      - name: Sync dependencies
        run: uv sync --extra sympozium --extra dev

      - name: Lint validator
        run: uv run -- ruff check agents/sympozium/scripts/

      # Field-level checks the Helm templates cannot do: skills, MCP server
      # names and prefixes, tool/server coherence, schedule enums, orphaned
      # prompts. Each of these fails silently at runtime if wrong.
      - name: Validate ensemble sources
        run: uv run python agents/sympozium/scripts/validate.py

      - name: Set up Helm
        uses: azure/setup-helm@v4

      # Installed from the release tarball rather than a third-party action, and
      # unpinned like azure/setup-helm above: renovate.json ignores **/.github/**,
      # so a pinned version here would go stale without anything noticing.
      - name: Set up helmfile
        run: |
          version=$(curl -fsSL https://api.github.com/repos/helmfile/helmfile/releases/latest \
            | jq -r .tag_name | sed 's/^v//')
          curl -fsSL "https://github.com/helmfile/helmfile/releases/download/v${version}/helmfile_${version}_linux_amd64.tar.gz" \
            | sudo tar -xz -C /usr/local/bin helmfile
          helmfile version

      # The templates assemble the Ensembles from projects/ at render time —
      # nothing is generated into the repository — so rendering is the build,
      # and a render failure here is a broken deploy.
      #
      # Via helmfile, not `helm template`, because that is the path ArgoCD takes:
      # its CMP runs argo-cd-helmfile.sh. The difference is not cosmetic —
      # helmfile renders values/default.yaml.gotmpl as a Go template *before*
      # Helm parses it as YAML, comments included, so a literal brace pair
      # anywhere in that file is an undefined-function error that `helm template`
      # never sees. That is exactly how a comment broke the ArgoCD sync on
      # 2026-08-23 after CI had passed.
      - name: Render ensembles
        working-directory: agents/sympozium
        run: helmfile template

  mcp:
    name: MCP servers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python with uv
        run: uv python install 3.13

      - name: Sync dependencies
        run: uv sync --extra mcp --extra dev

      - name: Lint
        run: uv run -- ruff check agents/mcp/

      # The facts server exists so a 4B model never assembles a query. These
      # tests are what the nine prose validators in the sympozium tree used to
      # be: the fill inversion, the counter window, the nodename join, the byte
      # budget and the absence path are each asserted against the expression the
      # server actually sends rather than against English in a prompt.
      - name: Test
        run: uv run pytest agents/mcp/tests/ -q

      # The image must exist for both architectures or the server is unschedulable
      # on the arm64 nodes, and an unreachable MCP server fails silently.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image for both architectures
        uses: docker/build-push-action@v5
        with:
          context: agents/mcp
          push: false
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
